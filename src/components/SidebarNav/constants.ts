// Mirrors the --collapsed-width / --expanded-width custom properties in
// SidebarNav.css. Exposed on SidebarNavStyles so consumers can lay out content
// next to the nav; keep both sides in sync.
export const collapsedWidth = "5.6rem";
export const expandedWidth = "24rem";
